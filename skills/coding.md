# Coding skill

Use `list_files` and `read_file` to understand the workspace before editing.
Use `write_file` for source code, tests, configuration, and documentation. Run
focused checks with `run_command` after changes. Never overwrite unrelated work.
Report the paths changed and any checks that could not be run.
